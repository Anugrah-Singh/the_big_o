[project]
name = "database"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mariadb>=1.1.12",
    "mcp[cli]>=1.9.1",
    "python-dotenv>=1.1.0",
]
